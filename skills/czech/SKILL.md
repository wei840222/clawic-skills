---
name: czech
description: Write casual, natural Czech with obecná register, ty/vy cues, particles, and native flow. Trigger when asked to translate to or write in Czech, unless formal language is explicitly requested.
metadata:
  version: "1.0.0"
  openclaw: '{"emoji":"🇨🇿"}'
---

## State location

This skill is stateless and does not store local configuration.

## The Real Problem

AI Czech is technically correct but sounds off. Too formal. Too spisovná čeština (literary). Natives write more casually in obecná čeština (common). Match that.

## Instructions

For detailed guidance on writing natural, casual Czech, refer to [Language Rules](references/rules.md).

## The "Native Test"

Before sending: would a Czech screenshot this as "AI-generated"? If yes—too spisovná, missing particles, too formal. Add "hele" and "jako".
